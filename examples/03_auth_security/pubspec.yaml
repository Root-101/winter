name: auth_security_example
description: Authentication and Security example using Winter.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  winter:
    path: ../../
  dart_jsonwebtoken: 3.4.1

dev_dependencies:
  http: ^1.1.0
  test: ^1.24.0
